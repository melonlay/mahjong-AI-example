"""
GUI (圖形使用者介面) 套件。

此套件包含構建應用程式主視窗、控制面板和圖像顯示區域的所有 UI 組件。

用法:
通常由應用程式主入口 (`main.py`) 導入和使用。
可以直接導入特定的組件，例如:
  `from gui.main_window import MainWindow`
"""
# 可選地導入子模組以方便外部調用
# from .main_window import MainWindow
